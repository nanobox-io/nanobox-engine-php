#!/bin/bash
# -*- mode: bash; tab-width: 2; -*-
# vim: ts=2 sw=2 ft=bash noet

# $1 = code_dir

[[ -n "$(ls ${1}/*.php 2>/dev/null)" ]] && exit 0
for subdir in $(find ${1} -type d); do
	[[ -n "$(ls ${subdir}/*.php 2>/dev/null)" ]] && exit 0
done
exit 1